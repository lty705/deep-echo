# 深窟回声 (Deep Echo) — 设计交付概览

## 做了什么
基于用户给定方向(**手机触屏 · 动作 Roguelike · 完整游戏 · 探索/发现感**),产出一份可落地的游戏设计文档(GDD v0.1),把"好玩"拆成可执行的机制与平衡方案。

## 关键决策
- **核心乐趣假设**:玩家偶然触发/捡到未说明机制,经自己试错搞明白后能反过来利用它。验证标准=盲测前 3 局有 ≥3 人说"原来还能这样"。
- **5 条设计支柱**:未知密度、发现即奖励、单手触屏手感、失败不归零、好奇 vs 风险。
- **灵魂机制"无名遗物"**:只显示图标、不写效果,玩家靠实战推断,首次推断自动入图鉴——把探索支柱做成动作循环的一部分。
- **双货币经济**:碎片(局内清零)+ 回响(元进度),含来源/沉没模型与通胀监控指标。
- **平衡表**:16 项数值全部以变量接入、标记 `[PLACEHOLDER]` 待实测,不硬编码。

## 交付文件
- `GDD_深窟回声.md` — 完整设计文档(支柱/循环/5 机制规格/经济/引导/备注)
- `balance_深窟回声.csv` — 平衡表,可直接导入 Excel / 接代码配置
- `overview.md` — 本文件

## 下一步
1. 纸面原型跑 5 局验证发现密度
2. Vertical Slice:1 角色 + 3 层 + 5 遗物 + 1 Boss
3. 5 人盲测,只看"原来如此"时刻
4. `[PLACEHOLDER]` 数值进入实测,先手感后经济

---

## 已实现:可玩 Demo + 选角系统(2026-07-24 更新)

设计文档之外,已落地一个**纯前端 Canvas 动作 Roguelike 可玩 Demo**(`deep-echo/`),并持续迭代:

- **黑屏/沙箱容错**:沙箱 iframe 中 `localStorage` 抛 `SecurityError` 时安全降级,渲染循环不崩。
- **打击感**:挥砍弧光、命中冲击环、火花、敌人挤压、顿帧震屏、子弹拖尾。
- **图形化造型**:玩家=提灯幽灵(光锥+旋转水晶壳),7 种敌人独立剪影。
- **祭坛交互**:移动端点击吞事件修复(按钮移出 `backdrop-filter` 容器 + `bindTap` 双绑定)。
- **选角系统(本次新增)**:开始/每日种子进入选角界面,4 职业数据驱动 ——
  - 坚壁守卫(坦克,+40 血/重击/慢速)、疾风游侠(敏捷,极速/薄血)、余烬咒师(远程火球)、影刃(近战,快攻+吸血)。
  - 职业差异经 `Player` 派生属性 + `Relics.recompute` 叠加,影响生命/速度/攻击/攻速/闪避冷却/半径/吸血/远程。
  - HUD 显示当前职业名与配色;选角偏好存 `localStorage`。

**公网地址**:https://6371484eea7640ac889d18bf40cbe9e6.app.codebuddy.work
**关键文件**:`deep-echo/src/{config,entities,relics,game}.js`、`deep-echo/index.html`、`deep-echo/src/style.css`
